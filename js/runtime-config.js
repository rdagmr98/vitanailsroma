/* SMAM/AVES GhDb pattern: GitHub Contents API on data/*.json
   Token injected at Pages build from Actions secret — never commit a real token. */
window.VITA_GH = window.VITA_GH || {
  owner: "rdagmr98",
  repo: "vitanailsroma",
  path: "data",
  token: "",
};
